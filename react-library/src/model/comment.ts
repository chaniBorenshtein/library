import type { JSX } from "react/jsx-dev-runtime"
export type comment={
    map(arg0:(y:any)=>JSX.Element):unknown
    id:number
    content:string
    LocalDate:Date
    username:string
    date:Date
}